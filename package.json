{
  "name": "fcc-classroom-mode",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "cd server && npm install && node ./bin/www && cd ../",
    "heroku-post-build": "cd client && npm install --only=dev && npm install && npm run build"
  }
}
